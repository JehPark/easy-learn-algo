#include "basicsort.h"

void bubblesort(int *arr, int size)
{
    for (int last = size - 1; i > 0; i--)
    {
        for (int i = 0; i < last - 1; i++)
        {
            if (arr[i] > arr[i + 1])
                swap(arr, i, i + 1);
        }
    }
}